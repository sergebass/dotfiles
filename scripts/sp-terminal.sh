#!/bin/bash

# FIXME determine which of the terminals is present and then launch one of them
urxvt -vb +sb -bc -cr orange -fg '#c0c0c0' -bg '#002020' -fn 'xft:Inconsolata:style=bold:pixelsize=14:antialias=true:hintstyle=hintslight' -letsp -1 -fade 10
